//welcome we are testing the batches endpoint.

//imports


describe('Batches Test Suite', () => {
  test('GET Batches', () => {
    //test goes here
  });
});

// the next lines will go into the actual function, not the test
/**
 * getBatches takes in a trainer ID (there is no trainer ID, only trainer email) and returns a list of batch objects from the caliber mock api
 * @param event
 */
