# Make the associate lambda from an existingimage
AWSTemplateFormatVersion: 2010-09-09
Description: lambda for weekCategories lambda
Resources:
  weekCategoriesLambda:
    Type: AWS::Lambda::Function
    Properties:
      Code:
        ImageUri: !Join ["/",[!Join [".",[!Sub "${AWS::AccountId}", "dkr", "ecr", !Sub "${AWS::Region}", "amazonaws", "com"]], "weekcategories:latest"]]
      Role: arn:aws:iam::169748077841:role/service-role/getApprovedBooks-role-635ptj5r
      # Role: arn:aws:iam::855430746673:role/caliber-mobile-cf
      FunctionName: "handler"
      PackageType: "Image"